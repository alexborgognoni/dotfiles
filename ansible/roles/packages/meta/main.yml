---
# Role metadata for packages role

galaxy_info:
  role_name: packages
  description: Multi-manager package installation (apt, snap, cargo, pip, npm, brew, manual)
  author: Alex Borgognoni
  min_ansible_version: "2.9"
  platforms:
    - name: Ubuntu
      versions:
        - focal
        - jammy
        - noble

dependencies:
  - role: base
