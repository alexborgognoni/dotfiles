---
# Default values for secrets role
# These are overridden by group_vars/personal.yml and group_vars/work.yml

# SSH keys list (profile-specific)
ssh_keys: []

# GPG keys list (profile-specific)
gpg_keys: []

# User information (from group_vars/all.yml)
personal_email: "alex.bor0419@gmail.com"
personal_name: "Alex Borgognoni"
work_email: "alex.borgognoni@deutsche-boerse.com"
work_name: "Alex Borgognoni"
